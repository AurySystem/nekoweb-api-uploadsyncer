const auth = " "; // your api token here
const source = "./production/"; // source folder for incoming .mds
const cacheSources = "./.production/"; // cache for comparing on startup

module.exports = {
  auth,
  source,
  cacheSources
};